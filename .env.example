APP_PORT=8085

# Required bearer token for incoming API requests.
# Clients must send: Authorization: Bearer <token>
API_AUTH_BEARER_TOKEN=replace-with-a-long-random-token

# Optional startup webhook registration:
# comma/semicolon/newline separated URLs
WEBHOOK_URLS=http://192.168.55.73:3350/receive-msg

# Optional file path that contains webhook URLs (newline or CSV)
# WEBHOOKS_FILE=/data/webhooks.csv

# Optional bearer token used in outbound webhook requests
WEBHOOK_AUTH_BEARER_TOKEN=

# Optional startup metadata log fields
IMAGE_TAG=local
BUILD_SHA=dev
AUTHORIZED_WHATSAPP_IDS=98775758581839@lid

# Optional WhatsApp auto-recovery watchdog
# WHATSAPP_AUTO_RECOVER=true
# WHATSAPP_AUTO_RECOVER_INTERVAL_MS=30000
